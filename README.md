# portainer-nginxmanager-stack
Stack of portainer and proxy nginx manager services

## Start using nginx manager 
To start go http://localhost:81
Default user is `admin@example.com`
Default password is `changeme`
